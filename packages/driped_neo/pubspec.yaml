name: driped_neo
description: Driped Neo \u2014 Playful Neo-Brutal + Dark design tokens for Flutter. Mirrors @driped/neo CSS tokens.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
